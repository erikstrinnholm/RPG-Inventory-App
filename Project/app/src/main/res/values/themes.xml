<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RPGInventoryApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Base theme attributes -->
        <item name="colorButtonNormal">@null</item>
        <item name="android:actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
    </style>

    <style name="CustomButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">@color/TextColor</item>
        <item name="maxWidth">200dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:padding">12dp</item>
        <item name="android:fontFamily">serif</item>
    </style>

    <style name="OverflowMenuStyle" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="android:showAsAction">always</item>
    </style>
</resources>